# Report zo session – <téma>

- **Dátum / obdobie:** <od – do>
- **Vetva:** `ucet2/<téma>` · **commity:** `<prvý>..<posledný>` (`git log --oneline main..HEAD`)
- **Stav kontrol:** typecheck ✓/✗ · lint ✓/✗ · `npm test` <počet> ✓/✗ · `test:render` ✓/✗/nespúšťané

## 1. Čo je hotové
Po moduloch: čo používateľ (alebo rola v admine) teraz vie urobiť, kde to je (cesta v URL a v kóde).

## 2. Čo nie je hotové / rozpracované
Čo ostalo, v akom stave, čo presne chýba do dokončenia.

## 3. Zmeny mimo vlastnej oblasti
Súbory mimo `app/src/app/admin/` a `app/src/features/admin/`, ktoré sa menili, a prečo.

## 4. Databáza
Nové migrácie (názov súboru), nové tabuľky/stĺpce, zmeny stavového automatu projektu.

## 5. Rozhodnutia, ktoré som urobil sám
Technické voľby s krátkym odôvodnením (aby ich pôvodný účet vedel posúdiť).

## 6. Otázky a rozhodnutia pre používateľa
Čo treba rozhodnúť (biznis, právo, ceny), s odporúčaním.

## 7. Ako to overiť
Príkazy a kroky: napr. `npm run ui` → `/ui` → „Výroba“; prihlásenie do `/admin` ako rola X; čo sa má stať.

## 8. Odchýlky od pravidiel a známe chyby
Čo nesedí s `CLAUDE.md` / `docs/handoff/README.md` (napr. obrazovka pretečie pri 1280×720) a prečo.

## 9. Odporúčaný ďalší krok
Jedna až tri veci, ktoré by mali nasledovať.
